#pragma once
#include <SFML/Graphics.hpp>

/// <summary>/// Returns whether u should render static and render it if yes/// </summary>/// <param name="window"></param>/// <returns></returns>

bool renderStaticOnUpdate(sf::RenderWindow& window);
void renderGeneralStatic(sf::RenderWindow& window);